const jwt = require('jsonwebtoken');
const SmitStore = require('../../dist/index.cjs').Build;

const sdk = SmitStore({
  apiKey: 'test_123',
});

describe('cart resource', () => {
  test('get contents from existing cart', () => {
    //
  });
});
